services:
  - type: web
    name: xiaoke
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: QQ_APP_SECRET
        sync: false
